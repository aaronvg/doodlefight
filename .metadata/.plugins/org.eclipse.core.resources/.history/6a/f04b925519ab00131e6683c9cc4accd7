package entities;

import com.badlogic.gdx.Gdx;


/**
 * This class controls all input to the game.
 * It also converts drawings to box2D objects and manages
 * drawn paths.
 * @author Aaron
 *
 */
public class DrawerManager {

	boolean newPath;
	public DrawerManager() {
		newPath = true;
	}
	
	
	public void update() {
		if(Gdx.input.isTouched()) {
			// Start drawing path.
			
		}
		else {
			// if we were drawing a path, save it now into our buffer.
		}
	}
	
	
	
	
	
	
}
